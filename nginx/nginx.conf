# Legacy placeholder.
#
# This project no longer uses a dedicated host nginx configuration with
# separate app/admin/api domains. Production traffic is routed through the
# shared `gateway` project to:
#
# - /app   -> forever-frontend:80
# - /admin -> forever-admin:80
# - /api   -> forever-backend:8000
#
# See:
# - ../README.md
# - ../../gateway/config/routes.yml
